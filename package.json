{
  "name": "argiope.nvim",
  "private": true,
  "description": "Development and test tooling for argiope.nvim",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/m4r-sh/argiope.nvim.git"
  },
  "homepage": "https://github.com/m4r-sh/argiope.nvim#readme",
  "bugs": {
    "url": "https://github.com/m4r-sh/argiope.nvim/issues"
  },
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "deps": "bun scripts/bootstrap-tests.js",
    "dev": "bun run deps && bun scripts/run-dev.js",
    "themes:sync": "bun scripts/sync-themes.js",
    "test": "bun run deps && bun run test:native:only && bun run test:plenary",
    "test:plenary": "bun scripts/run-tests.js tests",
    "test:native": "bun run deps && bun run test:native:only",
    "test:native:only": "bun scripts/native-smoke.js",
    "test:indent": "bun run deps && bun scripts/run-tests.js tests/indent",
    "test:integration": "bun run deps && bun scripts/run-tests.js tests/integration",
    "test:model": "bun run deps && bun scripts/run-tests.js tests/model",
    "test:highlight": "bun run deps && bun scripts/run-tests.js tests/highlight",
    "test:injections": "bun run deps && bun scripts/run-tests.js tests/injections",
    "test:config": "bun run deps && bun scripts/run-tests.js tests/config",
    "test:health": "bun run deps && bun scripts/run-tests.js tests/health"
  }
}
