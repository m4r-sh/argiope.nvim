# Filetype integration

Argiope attaches from `after/indent/javascript.lua`. Loading after Neovim's
JavaScript indent script lets the plugin preserve that script as the fallback
for ordinary JavaScript and interpolation expressions.

This directory is reserved for filetype behavior that does not belong to the
indentation layer.
